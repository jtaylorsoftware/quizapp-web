
user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

    server {
        listen      80;
        server_name makequizzes.online;
        return      301 http://www.makequizzes.online$request_uri;
    }

    server {
        listen      80;
        server_name www.makequizzes.online;

        location /api/v1/ {
            proxy_pass http://www.makequizzes.online:8080/api/v1/;
        }

        location /api/v2/ {
            proxy_pass http://www.makequizzes.online:8080/api/v2/;
        }

        location / {
            root /usr/share/nginx/html;
            try_files $uri /index.html;
        }
    }
}
